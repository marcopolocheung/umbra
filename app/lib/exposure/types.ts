// Stable subpath for consumers that keep domain types in a directory.
export {
  type ExposureObjective,
  type ExposureSettings,
  type ExposureLocation,
  type ManualWind,
  type ResolvedExposureContext,
  type WindProvenance,
  type WindSource,
} from "../exposure";
